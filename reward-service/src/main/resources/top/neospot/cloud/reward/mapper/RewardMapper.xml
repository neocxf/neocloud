<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.neospot.cloud.reward.mapper.RewardMapper">
    <select id="queryOrderById" resultType="top.neospot.cloud.reward.entity.Reward">
        select * from reward where id = #{id}
    </select>

</mapper>